---
description: Orient at the start of a Blood & Grit Code session — read the handoff doc, check git/branch state, restate the workflow rules
argument-hint: (no args)
allowed-tools: Bash(git status:*), Bash(git branch:*), Bash(git log:*), Bash(git checkout:*), Bash(git fetch:*), Read, Grep
---

You are starting a fresh Code session on **Blood & Grit** (`C:\Users\Cole\Desktop\Git\BloodAndGrit`),
a western-horror TTRPG with two independent deliverables: **three HTML companion books**
(built from lean Python sources) and **The Keeper's Table** (C#/.NET 8 WinForms desktop app).
Do NOT make any edits yet — this is orientation only. Work through these steps, then brief me.

## 1. Read the handoff doc
Read `CLAUDE.md` (the master handoff; `preferences.md` is a duplicate — read one). It's long,
so grab the **version table + newest Changelog entries** first (the Changelog is "newest first"
under `## Changelog`, the current-versions line is the last line of the file). Pull out:
- The **current versions**: Player's Book · Keeper's Book · Bestiary · The Keeper's Table app.
- The newest **Changelog** entries — what the last sessions actually changed.
- The **"How I like to work"** rules, the **Verification checklist**, and the standing
  Keeper's Table **landmines** — treat all of these as defaults for the session.

## 2. Review the git + branch state
Run (read-only):
- `git status` and `git branch --show-current` — where are we, is the tree clean?
- `git branch --no-merged main` — **any session branch NOT merged into `main`?** That usually
  means a prior session's merge-and-delete step was skipped — flag it and recommend a fix.
- `git log --oneline -8` — what landed recently.
- Note the "BloodAndGrit AutoSync" scheduled task (every 30 min + at logon) commits/pushes
  whatever branch is checked out; a clean tree "up to date with origin" is the normal state.

## 3. Restate the workflow I work under
Remind me (one line each) so we start aligned:
- **Session branches, no exceptions.** Before the first edit of *anything* (books, app, docs,
  tooling), create `session/<yyyy-mm-dd>-<short-topic>`. On success, `--no-ff` merge into
  `main` and delete the branch (local + origin). If it doesn't verify, abandon the branch.
- **Books: edit the lean sources, never the generated output.** Each book is one builder —
  its content lives inside its script. Edit `build_player.py` (the Player's Book HTML is the
  embedded `SRC` string), `build_keeper.py`, or `build_bestiary.py` (creatures included — the
  old `bestiary_extra.py` is merged in), plus the shared `nav_tools.py` / `perdition_map.py` /
  `pag_patch.py`, then rebuild (player first; the other two read `blood-and-grit.html`). **Never flatten the
  rich HTML/CSS design to markdown**, and never hand-edit generated self-contained HTML or PDFs.
  Contents/Index are generated by `nav_tools.py` — don't hand-maintain them. **Version cascade:**
  a Player's Book bump means updating the match strings in `build_keeper.py` *and*
  `build_bestiary.py`, or their covers keep the old Player version.
- **The Keeper's Table (C# app): edit `KT/source`**, not the delivered `BloodAndGrit-Keepers-Table/`
  (that + the zip are regenerated from `KT/`). App additions go in `Data/tables_extra.json`
  (merged at load) — never fold them into the book-extracted `tables.json`. **Landmine:** never
  set `SplitterDistance`/`Panel1MinSize`/`Panel2MinSize` at construction — use the `Split()`
  helper in `MainForm.cs` (it defers geometry to first `SizeChanged`), or it throws only when
  rendered on Windows.
- **No PDFs unless I explicitly say "save to PDF."** Default deliverable is lean source +
  self-contained HTML. Responsive/phone-friendly stays a priority.
- **Verify before handing back, then update CLAUDE.md** (version table + Changelog + any
  section that changed).

## 4. Brief me
Give me a compact summary: current versions (all four), what the last session did, the
branch/merge state (call out anything unmerged or dirty), any open thread worth picking up,
and — if we're on a leftover session branch — what you recommend. Then wait for my direction.
Don't start editing until I say what to work on.

### Toolchain reminders (for when work does start)
- Python: use the full path `%LOCALAPPDATA%\Programs\Python\Python312\python.exe` (bare `python`
  may hit the Store stub). Playwright drives system Edge via `channel="msedge"`.
- Book verifiers: `measure_index.py` (Player's Book — parity/clip/h-scroll/anchors, re-patches
  index statics) and `measure_book.py <file>` (Keeper's Book / Bestiary). Force `zoom:1` per
  `.page`. Node is absent — skip `node --check`; exercise JS by real rendering.
- App loop: edit `KT/source` → `dotnet build -c Release` (expect 0/0) → run `KT/smoke`
  (`dotnet run -c Release`) → launch & screenshot the exe → `dotnet publish -c Release -r win-x64
  --self-contained -o publish` → sync the delivered folder + re-zip → bump app version + Changelog.
